import { createSelector } from 'reselect'

// export const getStuff = createSelector(
//   state => state.stuff,
//   state => state.otherStuff,
//   (stuff, otherStuff) => makeAwesomeStuff(stuff, otherStuff)
// )
